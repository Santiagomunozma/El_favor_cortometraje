{
  "name": "el-favor-bitacora",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix el-favor-bitacora run dev",
    "build": "npm --prefix el-favor-bitacora install && npm --prefix el-favor-bitacora run build",
    "preview": "npm --prefix el-favor-bitacora run preview"
  }
}
